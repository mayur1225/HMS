//import { Router } from 'express';


// const { getUserByName } = require('../../middleware/check-user-existance');

// const { generateToken } = require('./generate-token');

// const { validateToken } = require('./validate-token');
// const { logout } = require('./logout');

//const app = Router();

//const { passportAuthenticate } = require('../../middleware/passport-autheticate');


// login Api
//app.route('/login').post(getUserByName, passportAuthenticate, generateToken);

// logout Api
//app.route('/logout').put(logout);

// validate Token
//app.route('/token').get(validateToken);